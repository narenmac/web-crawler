apiVersion: v1
kind: Namespace
metadata:
  name: web-crawler
---
apiVersion: v1
kind: Secret
metadata:
  name: azure-storage-secret
  namespace: web-crawler
type: Opaque
# Populated by CI/CD pipeline or manually:
# kubectl create secret generic azure-storage-secret -n web-crawler \
#   --from-literal=connection-string="<STORAGE_CONNECTION_STRING>"
stringData:
  connection-string: "REPLACE_WITH_ACTUAL_CONNECTION_STRING"
