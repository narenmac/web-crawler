name: CD - Deploy to Production

on:
  release:
    types: [published]

env:
  DOCKER_HUB_USERNAME: ${{ secrets.DOCKER_HUB_USERNAME }}
  IMAGE_TAG: ${{ github.event.release.tag_name }}

jobs:
  build-and-push:
    name: Build & Push Docker Images (Prod Tag)
    runs-on: ubuntu-latest
    strategy:
      matrix:
        service:
          - name: frontend
            context: ./frontend
          - name: api-gateway
            context: ./api-gateway
          - name: crawler-orchestrator
            context: ./crawler-orchestrator
          - name: url-fetcher
            context: ./url-fetcher
          - name: content-parser
            context: ./content-parser
    steps:
      - uses: actions/checkout@v4

      - uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_TOKEN }}

      - uses: docker/build-push-action@v5
        with:
          context: ${{ matrix.service.context }}
          push: true
          tags: |
            ${{ env.DOCKER_HUB_USERNAME }}/web-crawler-${{ matrix.service.name }}:${{ env.IMAGE_TAG }}
            ${{ env.DOCKER_HUB_USERNAME }}/web-crawler-${{ matrix.service.name }}:prod-latest

  deploy:
    name: Deploy to Production (Terraform)
    needs: build-and-push
    runs-on: ubuntu-latest
    environment: production
    defaults:
      run:
        working-directory: infrastructure
    steps:
      - uses: actions/checkout@v4

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: '1.7.0'

      - name: Azure Login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Terraform Init
        run: terraform init

      - name: Terraform Plan
        run: |
          terraform plan \
            -var-file="environments/prod.tfvars" \
            -var="frontend_image_tag=${{ env.IMAGE_TAG }}" \
            -var="api_gateway_image_tag=${{ env.IMAGE_TAG }}" \
            -var="orchestrator_image_tag=${{ env.IMAGE_TAG }}" \
            -var="url_fetcher_image_tag=${{ env.IMAGE_TAG }}" \
            -var="content_parser_image_tag=${{ env.IMAGE_TAG }}" \
            -out=tfplan

      - name: Terraform Apply
        run: terraform apply -auto-approve tfplan
